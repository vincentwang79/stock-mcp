# 项目基本规则

本文件定义所有人员、AI Agent、自动化流程和发布操作都不得违反的基本规则。若任务规格、实现便利或临时指令与本文件冲突，以安全边界、数据真实性和可复现性为优先。

## 一、产品边界

- 本项目只提供沪深主板 A 股研究、候选解释、观察列表和复盘记录。
- 永不提供自动交易、券商接入、订单、余额、仓位、收益承诺、推送或持续盘中监控。
- ChatGPT 只能解释服务端已经生成的证据，不能新增候选、改变排名、修改分数或绕过策略治理。

## 二、事实与证据

- 任何候选结论必须来自已验证的规范化市场快照和明确的不可变策略版本。
- 单份日报只能使用一个完整价格源；不得拼接不同来源来掩盖缺失数据。
- 缺失、过期、不完整或无法验证的数据必须显式降级或失败，不得猜测、补写或伪装成成功。
- 所有价格、成交量、成交额、时间、来源、策略版本和流水线版本必须可追溯。

## 三、确定性与前视控制

- 最高验收边界为：`规范化市场快照 + 不可变策略版本 → 已验证的每日报告`。
- 相同快照和策略版本必须产生完全相同的结果。
- 历史回放不得读取目标交易日之后的数据；前 20 个交易日只用于预热。
- 防御市场返回零候选是合法成功结果，不能为了凑数降低规则或引入模型猜测。

## 四、策略治理

- 策略阈值必须形成不可变版本；新版本只能以提案状态创建。
- 激活前必须完成完整三年交易日历的逐日回放，并将参数哈希和数据集哈希写入证明。
- 策略激活必须同时具有主机侧一次性批准和用户明确确认；MCP 参数本身不能充当人的批准证明。
- 激活新版本不得改写历史日报、候选、证据或复盘事件。

## 五、写操作与个人数据

- 所有公开写操作必须使用持久化幂等键或只追加事件，并对幂等键冲突返回结构化错误。
- 观察列表保留用户顺序；候选事件必须使用受限状态、日期、可选价格和原因。
- 不接收、不保存账户、仓位、订单数量、券商凭证或其他交易执行信息。

## 六、密钥与权限

- 真实密钥不得进入代码、测试、日志、问题单、Agent 提示、交接文档、诊断包或版本控制。
- 生产密钥只允许通过 Windows 安全交互脚本输入，并存放在最小权限 ACL 保护的文件中。
- 应用服务和 Tunnel 服务使用不同的低权限身份；程序文件只读，数据写权限按职责分离。
- MCP 只监听本机回环地址；Windows 不开放入站端口，Tunnel 只使用出站 HTTPS。

## 七、Agent 协作

- 主 Agent 独占公共类型、数据库核心 Schema、依赖版本、跨模块接口、集成和提交。
- Sub-agent 只能修改任务卡授权的目录，不得回退他人改动，不得自行扩大产品范围。
- Reviewer 只读并报告可复现问题，不直接修改代码。
- 任何实现都必须先建立正确失败的测试，再完成修复和回归验证。
- 遇到权限、环境或上下文阻塞时必须显式报告并按规则交接，不得伪造完成、测试结果或外部发布状态。

## 八、测试与发布

- 标准测试使用固定、脱敏、带来源时间的夹具，不访问实时行情或生产密钥。
- 发布前必须通过完整测试、格式检查、依赖锁检查、构建、差异检查和密钥扫描。
- 静态 Windows 脚本测试不能替代干净 Windows Server VM 上的 PowerShell、WinSW、ACL、升级回滚、重启和 Tunnel 验收。
- 未经真实环境验证的事项必须明确标记为外部门禁，不得描述为已经通过。
- Windows 发布 ZIP 必须在解压或执行任何脚本前，使用独立可信渠道发布的 SHA-256 摘要完成校验。
- 从受控 Git 源码直接安装时，必须拒绝脏工作区、记录确切提交和远程地址、验证固定工具哈希，并将运行副本写入独立版本目录；该路径不适用于没有 Git 信任链的离线发布。
